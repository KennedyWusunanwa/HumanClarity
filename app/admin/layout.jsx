export const metadata = {
  title: 'Admin · HumanClarity',
  description: 'HumanClarity admin dashboard',
  robots: { index: false, follow: false, nocache: true },
};

export default function AdminLayout({ children }) {
  return children;
}
